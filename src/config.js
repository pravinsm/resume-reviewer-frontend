// const SERVER_URL = "http://localhost:8000";
const SERVER_URL = "https://resume-reviewer-backend.onrender.com"

const url = {
    UPLOAD: SERVER_URL + "/upload",
    RESUMES: SERVER_URL + "/resumes",
}

export const config = { url };